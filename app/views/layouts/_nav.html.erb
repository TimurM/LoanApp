<nav>
    <div class="container">
      <span>QuickLoans</span>


    <div class="login">
      <% if signed_in? %>

      <%= current_user.email %>
      <%= link_to "Sign out", session_url, method: :delete %>
      <% else %>
      <%= link_to "Sign In", new_session_url %>
      <%= link_to "Sign Up", new_user_url %>
      <% end %>
    </div>
  </div>
</nav>
